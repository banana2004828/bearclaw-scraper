# 熊爪采集器 · 使用说明（自研版）

> **自主开发声明**：本工具由「小熊工具箱」完全自研，代码独立编写，**不包含任何第三方爬虫框架源码**，不构成任何开源项目的衍生作品，无第三方开源协议约束。
> 技术路线采用通用浏览器自动化方案（Playwright），采集逻辑、数据处理、安全机制均为自研实现。
> 代码著作权归「小熊工具箱」所有。

---

## 一、功能

浏览器自动化采集**小红书**关键词搜索结果（笔记标题/作者/点赞/收藏/链接），数据落 SQLite + CSV。

| 项 | 说明 |
|---|---|
| 当前平台 | 小红书（网页版搜索） |
| 采集内容 | 标题、作者、点赞、收藏、链接 |
| 输出 | SQLite（data/bear_scraper.db）+ CSV（data/） |
| 登录方式 | 浏览器扫码登录一次，登录态缓存复用 |

## 二、快速开始

### 第 1 步：双击 `启动.bat`
自动检测 Python → 建虚拟环境 → 装 Playwright → 就绪。

### 第 2 步：输入采集命令
```bash
# 采集 20 条
python 熊爪采集.py --keyword 儿童玩具 --limit 20

# 采集 50 条，只导出 CSV
python 熊爪采集.py --keyword 健身 --limit 50 --save csv

# 只存数据库
python 熊爪采集.py --keyword 咖啡 --limit 30 --save db
```

### 第 3 步：小号扫码
首次运行弹出浏览器 → 搜索页出现登录墙 → **用一次性小号**扫码 → 自动继续采集 → 登录态缓存，下次免登录。

### 第 4 步：看数据
`data/` 目录下 CSV 可直接 Excel 打开；数据库文件可用 SQLite 工具查看。

## 三、安全机制（内置，不可关闭）

| 机制 | 值 | 说明 |
|---|---|---|
| 随机延迟 | 2-5 秒 | 每次滚动/请求间随机等待 |
| 单次限量 | ≤50 条 | 防高频特征 |
| 单并发 | 1 | 永不并发 |
| 登录态缓存 | 本地文件 | 避免反复扫码（反复扫码也是风险信号） |
| 自动化痕迹 | 已抹除 | 禁用自动化检测标志 |

## 四、⚠️ 封号风险（必读）

> 采集行为本质是"模拟真人"，平台风控仍可能识别。**账号安全第一**。

1. **只用小号**——主号一旦被风控，权重受损甚至封禁，不可逆。
2. **控制节奏**——单次 ≤50 条，两次采集间隔 30 分钟以上，不要连着跑。
3. **不要多开**——同平台多个采集窗口 = 自投罗网。
4. **关注 IP**——同一 IP 高频访问会被限流；连续多轮建议换网络/代理。
5. **完成即退出**——采集完立即关浏览器，不留后台。

### 被封了怎么办
- 立即停止采集，退出浏览器。
- 小号被封就弃用换新号，**绝不用主号尝试**。
- 换 IP（重启路由器）后 24 小时再继续。

## 五、合规提示

- 本工具为**通用浏览器自动化技术演示/个人研究**用途。
- 采集小红书数据仍受平台用户协议约束，请遵守平台规则；商业化使用采集数据存在法律灰色地带（如《反不正当竞争法》相关判例），请自行评估风险。
- 抓取内容版权归原作者/平台所有，请勿用于恶意用途。

## 六、常见问题

**Q1：弹出浏览器但没反应？**
检查是否出现登录墙：如果有，扫码登录后自动继续。若一直白屏，等 10 秒或重试。

**Q2：采集到 0 条？**
① 关键词过冷；② 被限流（换 IP）；③ 登录态失效（删 cookie.json 重登）。

**Q3：提示 playwright 不存在？**
`启动.bat` 会自动装；若失败，手动执行：
```
pip install playwright
python -m playwright install chromium
```

**Q4：想扩展其他平台？**
当前版本聚焦小红书。后续版本将支持抖音/微博等（架构已预留平台扩展位）。

---

*熊爪采集器 v0.1 · 由小熊工具箱自主开发 · 2026-08-27*
